# Examples

These examples are built as **PlatformIO projects**.

They modify TFT_eSPI build definitions via `platformio.ini`.

## ⚠️ Arduino IDE Users

If using Arduino IDE, you must manually configure:

TFT_eSPI/User_Setup.h

To match your specific display and pin configuration.

The examples assume correct TFT_eSPI configuration before compiling.
